<Window x:Class="PowerCmd.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PowerCmd"
        mc:Ignorable="d"
        Title="PowerCmd" Height="500" Width="900" WindowStartupLocation="CenterScreen">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        
        <TextBox x:Name="Output" Background="Black" Foreground="White" FontFamily="Courier New" TextWrapping="Wrap" IsReadOnly="True"
                 HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />
       
        <TextBox Grid.Row="1" Margin="0,4,0,0" 
                 KeyUp="Input_OnKeyUp"
                 x:Name="Input"></TextBox>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,4,0,0">
            <Button Content="VS2015" Click="ButtonBase_OnClick"></Button>
        </StackPanel>
    </Grid>
</Window>
